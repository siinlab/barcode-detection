name: CI/CD Pipeline for development

on:
  push:
    branches: develop

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
   
    steps:
    
      - name: Checkout Repository
        uses: actions/checkout@v2
        with:
          submodules: 'true'
          token: ${{ secrets.CI_TOKEN }}

      - name: Set up Docker
        uses: docker/setup-buildx-action@v1
    
      - name: Build and Push Docker Image
        run: |
          cat .env
          cd scripts/docker-management
       
      - name: Copy image to remote server
        run: |
          export IMAGE_NAME="ai-engine-barcode"
          export IMAGE_TAG=$(cat ./VERSION)
          echo $IMAGE_NAME:$IMAGE_TAG
          docker save $IMAGE_NAME:$IMAGE_TAG | bzip2 | pv | ssh ai-engine-develop docker load
    


